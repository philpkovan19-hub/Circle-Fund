{
  "name": "circlefund",
  "version": "1.0.0",
  "description": "CircleFund - rotating savings circles (ROSCA / digital chit fund) on BOT Chain",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network botchain_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network botchain"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "chai": "^4.3.10",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
